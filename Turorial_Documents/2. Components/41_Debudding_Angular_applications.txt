Debugging Angular applications
	- Addning Consile statement in code to debug
	- Checking the sources tab in devloper consle log(browser) and adding breakpoints	
	- Add the json pipe in template to check if the values are correct
	- Always unsubscibe the subscription - always, else it will lead to memory leaks.
		- if we dont unsubscribe it will do memory leak
	- If making HTTP calls -> Inspects the Network tab for data being sent/recived
	- Alwys "Lint" to make sure - bad code does not peek in
		ng lint -> check the coding standards 

